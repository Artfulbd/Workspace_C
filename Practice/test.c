#include <stdio.h>
#include<math.h>
char main ()
{
    int n, i, ans  = 0;
    /*printf("Enter the value of n: ");
    scanf("%d",&n);
    for(i = 1 ; i<=n ; i++)
    {
       ans += i;

    }
    printf("Final result %d",ans);*/

    int z = pow(2,3);
    printf("%d", z );

    return 'L';
}
