#include<stdio.h>

main()
{
    int s;
    printf(" Enter array size: ");
    scanf("%d",&s);
    char name[s];
    int i;
    for(i=0;i<s;i++){
        scanf("%c",&name[i]);
        fflush(stdin);
    }
    for(i=0;i<s;i++){
        printf("%c",name[i]);
    }
}
