//array of string
#include <stdio.h>
int main()
{
    char *str[5];
    str[0]="Hello";
    str[1]="world";
    str[2]="pessimistic";
    str[3]="wanderer";
    str[4]="GD bye";
    int i;
//puts ();
    for (i=0; i<5; i++)
    {
        puts (str[i]);
    }
    printf("%s %s %s %s %s.",str[0],str[1],str[2],str[3],str[4]);
    return 0;
}
